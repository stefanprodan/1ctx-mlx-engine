# Copy to scripts/staging.env (git-ignored) and fill in. Sourced by
# scripts/staging.sh, scripts/preview.sh and the commands in
# docs/internal/staging.md.
STAGING_SSH=staging                     # ssh alias or user@host of the staging Mac
STAGING_HOST=staging.example.ts.net     # its Tailscale name (or IP)
